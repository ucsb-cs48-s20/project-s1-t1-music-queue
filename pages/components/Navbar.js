import React from "react";
import "./style.css";

const Navbar = () => (
  <div class="navbar">
    <a class="active" href="#">
      Input to MongoDB Database
    </a>
    <a href="#">Retrive Data</a>
  </div>
);

export default Navbar;
